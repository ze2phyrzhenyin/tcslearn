# Labs

Labs are small Python experiments that support intuition. They are not proofs.

Each lab should:

- run directly with `python path/to/lab.py`;
- use standard library unless `numpy` is justified;
- include `assert` tests;
- state which concept or example it illustrates;
- avoid heavy dependencies and long runtimes.

