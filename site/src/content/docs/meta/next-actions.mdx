---
title: "Next Actions"
description: "Concrete next actions for study and Codex feedback."
---

> **Generated page.** This page is generated from repository learning materials. Edit the source file, not this generated page.
>
> Source: `state/next_actions.md`

# Next Actions

## Immediate Week 2 Start

1. Start with `notes/week02/day01-specifications-sorting-lower-bounds.md`.
2. Handwrite the sorting problem specification: input, output, precondition, postcondition, and input size.
3. Do `exercises/week02/day01_exercises.tex` problems 1-8 before reading solutions.
4. Focus on the full proof of comparison sorting lower bound via decision tree.
5. Run `python3 labs/week02/sorting_and_lower_bounds.py` and write one sentence explaining why the lab is not a proof.
6. Record mistakes in `state/mistakes_log.md`, especially loop invariant and lower-bound model errors.

## After Day 1 Study

1. Redo insertion sort loop invariant without looking.
2. Re-solve the merge sort recurrence with a recursion tree.
3. Explain why counting sort does not violate the comparison lower bound.
4. Copy `context/next_codex_prompt.md` to request diagnostic feedback.

## Weekly

1. Run each Week 2 lab after its corresponding day.
2. Use `review/week02/proof_patterns_week02.md` as the main proof-pattern checklist.
3. Take `review/week02/self_test_week02.md` under a 120-minute timer after Day 7.
4. Use `review/week02/mistakes_to_watch.md` to classify errors before generating Week 3.

## Web Site

1. Run `make site-sync` after changing notes, exercises, labs, review, resources, or state.
2. Run `make site-build` before deployment.
3. Production site is deployed at `https://tcs-selfstudy-os.vercel.app`.
4. The next web task should only sync or polish content; do not redesign the site.

## Deployment Maintenance

1. Git remote: `git@github.com:ze2phyrzhenyin/tcslearn.git`.
2. Vercel project: `tcs-selfstudy-os`.
3. Root deployment uses `vercel.json` to build `site/` and publish `site/dist`.
